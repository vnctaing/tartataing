name: Continuous Delivery

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:

    - uses: actions/checkout@v2
    - name: Install dependencies
      run: lein deps

    - name: Create uberjar
      run: lein uberjar

